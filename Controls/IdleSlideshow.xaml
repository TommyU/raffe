<UserControl x:Class="Raffe.Controls.IdleSlideshow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="800">
    <UserControl.Background>
        <!-- 与主窗口背景完全一致，保证无图片时遮住后面的名字 -->
        <RadialGradientBrush Center="0.5,0.3" RadiusX="0.8" RadiusY="0.7">
            <GradientStop Color="#3D0C00" Offset="0"/>
            <GradientStop Color="#1A0800" Offset="1"/>
        </RadialGradientBrush>
    </UserControl.Background>
    <Grid ClipToBounds="True">
        <Image x:Name="ImgBack"  Stretch="Uniform"/>
        <Image x:Name="ImgFront" Stretch="Uniform"/>
    </Grid>
</UserControl>
